# 端到端验收清单

本文档对应 `docs/开发流程文档.md` 的阶段 15：安装、文档与端到端验收。手动验收建议在一次性测试仓库或独立 worktree 中执行，避免污染正在开发的主工作区。

> TS 重构分支更新：当前权威验收先看本节。下面 Python 验收项保留为 v0.1 参考，直到对应功能迁移到 TypeScript 后再逐项替换。

## TS-0. 当前 TypeScript 闭环验收

- [x] 在 `codex/ts-refactor` worktree 中安装依赖。

```powershell
pnpm install
```

- [x] 运行基础验证。

```powershell
pnpm verify
```

当前证据：typecheck 通过，Vitest 14 个测试文件 54 个测试通过。

- [x] 验收 CLI 薄入口。

```powershell
pnpm --filter @tokendance/code-cli build
node packages/cli/dist/main.js --version
node packages/cli/dist/main.js doctor
node packages/cli/dist/main.js run "hello"
```

当前证据：版本输出 `0.2.0-ts.0`；doctor 输出 Node、cwd、platform；run 输出 `Mock response: hello`。

- [x] 验收 SDK buffered turn。

覆盖测试：`packages/sdk/tests/sdk.test.ts`

- [x] 验收 SDK streamed events。

覆盖测试：`packages/sdk/tests/sdk.test.ts`

- [x] 验收 AgentHub SDK 审批回调。

覆盖测试：`packages/sdk/tests/sdk.test.ts`

期望：`default` 模式下 `write_file` 等写入工具先产生 `requires_approval`；AgentHub 的 `approvalCallback` 返回允许后，runtime 记录最终 `allowed` 权限事件并执行工具；返回拒绝时工具不得执行。

- [x] 验收 AgentHub SDK event sink。

覆盖测试：`packages/sdk/tests/sdk.test.ts`

期望：runtime event 写入 transcript 后同步推给 SDK `eventSink`，AgentHub 可用同一 typed event union 驱动 UI 和后端事件广播。

- [x] 验收 AgentHub runtime event 映射。

覆盖测试：`packages/sdk/tests/agenthub-events.test.ts`

期望：`TDCodeEvent` 能稳定映射为 AgentHub 已有的 `run.agent.text_delta/tool_call/permission_requested/permission_decided/tool_result/result` 事件名；SDK 提供 `createAgentHubEventSink()` 便于直接接入 AgentHub emitter。

- [x] 验收 core permission decision。

覆盖测试：`packages/core/tests/permissions.test.ts`

- [x] 验收 core runtime tool loop 与 transcript append。

覆盖测试：`packages/core/tests/runtime.test.ts`

- [x] 验收 TS file tools。

覆盖测试：`packages/core/tests/file-tools.test.ts`

期望：`read_file/write_file/edit_file/glob` 都经 `ToolOrchestrator` 和 `PermissionEngine`；workspace 外路径被拒绝；glob 默认排除 `.git`、`.tokendance`、`node_modules`、`.env` 等内部或敏感路径。

- [x] 验收 TS PowerShell shell tool。

覆盖测试：`packages/core/tests/powershell.test.ts`、`packages/core/tests/shell-tools.test.ts`

期望：`run_powershell` 经 `ToolOrchestrator` 和 `PermissionEngine`；默认模式要求审批；`yolo` 可执行普通命令；`git reset --hard`、`Remove-Item`、`iwr | iex` 等高风险命令即使在 `yolo` 下也被工具执行层拒绝。

- [x] 验收 TS patch tool。

覆盖测试：`packages/core/tests/patch-tools.test.ts`

期望：`apply_patch` 支持简单 `*** Update File` 文本补丁；目标路径必须在 workspace 内；不支持的 patch 或找不到 old text 时返回可诊断错误且不改文件。

- [x] 验收 TS Git 只读工具。

覆盖测试：`packages/core/tests/git-tools.test.ts`

期望：`git_status/git_diff/git_log/git_branch` 通过真实临时 Git 仓库验证；`git_diff` 可按 workspace 相对路径过滤，拒绝 workspace 外路径；`git_review` 报告冲突标记和新增 TODO；`quality_gate` 复用 PowerShell 执行和风险分类。

- [x] 验收 OpenAI Responses provider adapter。

覆盖测试：`packages/core/tests/openai-provider.test.ts`

期望：adapter 创建 `POST /v1/responses` 请求；导出 function tools；解析 `output_text` 和 `function_call`；下一轮能发送 `function_call_output`；API error 返回可诊断异常。

- [x] 验收 Anthropic-compatible Messages provider adapter。

覆盖测试：`packages/core/tests/anthropic-provider.test.ts`

期望：adapter 创建 `POST /v1/messages` 请求；system prompt 单独映射；导出 `input_schema` tools；解析 `text` 和 `tool_use`；下一轮能发送 `tool_result`；API error 返回可诊断异常。

- [x] 验收 TS context builder。

覆盖测试：`packages/core/tests/context-builder.test.ts`

期望：system prompt first；包含 AGENTS.md、README.md 和 compact summary；只保留配置数量的 recent messages；本轮 user message 作为最后一条。

- [x] 验收 TS compact/resume。

覆盖测试：`packages/core/tests/compact-resume.test.ts`、`packages/sdk/tests/sdk.test.ts`

期望：transcript envelope 有递增 `seq`；compact 生成 `compact-0001.md`、`compact-0002.md`，summary 使用 `Range: seq x-y` 和事件数量；resume 可按最新 session 加载 recent transcript，并过滤未闭合 tool started/permission 事件；SDK `loadLatestThread` 暴露 recent transcript 给 AgentHub 调用方。

- [x] 验收 TS memory store。

覆盖测试：`packages/core/tests/memory.test.ts`

期望：项目 memory 写入 `.tokendance/memory/project.md`，全局 memory 写入 `<home>/.tokendance/memory/global.md`；ContextBuilder 能注入 memory 条目；不做自动抽取。

## 0. 验收前检查

- [ ] 在 Windows PowerShell 中进入项目根目录。

```powershell
Set-Location C:\Users\29332\Desktop\TokenDanceCode
git status --short
python --version
```

期望：Python 版本不低于 3.11；如果工作区已有未提交修改，先记录来源，不要回滚他人改动。

- [ ] 确认当前高层结构。

```powershell
Get-ChildItem src\tokendance
Get-ChildItem tests
```

期望：能看到 `cli`、`config`、`context`、`core`、`execution`、`git`、`models`、`permissions`、`storage`、`tasks`、`tools` 等目录。

期望：能看到 `src\tokendance\agents`、`src\tokendance\tools\subagent.py` 和 `src\tokendance\git\worktree.py`，并继续执行 subagent worktree 验收。

## 1. 安装验收

### Editable install

- [ ] 在干净环境或虚拟环境中安装。

```powershell
py -3.11 -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -e .
tokendance --version
tokendance doctor
```

期望：`tokendance --version` 输出 `tokendance 0.1.0`；`tokendance doctor` 输出 Python、OS、Shell、CWD。

### pipx install

- [ ] 从仓库根目录安装全局命令。

```powershell
pipx install .
tokendance --version
tokendance doctor
```

期望：全局 `tokendance` 可用；如已安装旧版本，先记录状态，再按本机 pipx 流程决定是否升级或重装。

## 2. 配置与 API key 验收

- [ ] 在当前 PowerShell 会话设置 key，不写入仓库。

```powershell
$env:OPENAI_API_KEY = "sk-..."
$env:ANTHROPIC_API_KEY = "sk-ant-..."
```

期望：真实 provider 构造时能读取对应环境变量；缺少 key 时 provider 报清晰的 `OPENAI_API_KEY is not configured.` 或 `ANTHROPIC_API_KEY is not configured.`。

- [ ] 在交互 shell 查看当前配置。

```powershell
tokendance
/config
/exit
```

期望：显示 `provider`、`model`、`permission_mode`、`executor_backend`、`project_state`。当前默认值应为 `openai`、`gpt-5.4`、`default`、`local`、`local`。

## 3. doctor 验收

- [ ] 运行顶层 doctor。

```powershell
tokendance doctor
```

期望：输出 Python、OS、Shell、CWD，并且不进入交互 shell。

- [ ] 在交互 shell 中运行 `/doctor`。

```powershell
tokendance
/doctor
/exit
```

期望：输出与顶层 doctor 相同类型的信息，session 能正常退出。

后续增强期望：doctor 应继续覆盖 API key、Git、PowerShell、配置文件和可写状态目录检查。

## 4. unittest 验收命令

- [ ] 运行全部测试。

```powershell
python -m unittest discover
```

- [ ] 运行核心单元测试。

```powershell
python -m unittest discover -s tests\unit
```

- [ ] 运行集成测试。

```powershell
python -m unittest discover -s tests\integration
```

- [ ] 针对常见风险点运行定向测试。

```powershell
python -m unittest tests.unit.cli.test_main
python -m unittest tests.unit.cli.test_commands
python -m unittest tests.unit.permissions.test_engine tests.unit.permissions.test_powershell
python -m unittest tests.unit.tasks.test_task_store tests.unit.tasks.test_todo_store
python -m unittest tests.unit.tools.test_task_todo
python -m unittest tests.unit.git.test_review_revert_quality
python -m unittest tests.unit.agents.test_manager tests.unit.git.test_worktree tests.unit.tools.test_subagent
```

期望：真实 provider 集成测试在未设置 `TOKENDANCE_RUN_MODEL_INTEGRATION=1`、对应 API key 和测试模型名时跳过。

## 5. 权限模式验收

- [ ] 在交互 shell 中切换权限模式。

```text
/permissions
/permissions safe
/permissions default
/permissions auto
/permissions yolo
```

期望：每次切换都有明确提示；非法值返回 `Usage: /permissions default|safe|auto|yolo`。

- [ ] 验收 PowerShell 风险分类。

```powershell
python -m unittest tests.unit.permissions.test_engine tests.unit.permissions.test_powershell
```

期望：只读命令允许，未知命令需要确认，高风险命令拒绝；`yolo` 不绕过高风险拒绝。

- [ ] 人工验收写入边界。

期望：工作区内读取允许；工作区外写入拒绝；`safe` 模式下工作区内写入需要确认；`default`、`auto`、`yolo` 模式下工作区内写入允许。

## 6. 新会话、resume、transcript、compact

### 新会话

- [ ] 启动并正常退出 session。

```powershell
tokendance
hello from e2e
/status
/exit
```

期望：普通消息得到 mock provider 回显；`/status` 显示 session、mode、permissions、provider、model；`.tokendance\sessions\<session-id>\session.json` 和 `transcript.jsonl` 被创建。

### resume

- [ ] 恢复最近 session。

```powershell
tokendance resume
```

期望：输出 `Resumed session <id> with <n> recent transcript events.`。

- [ ] 使用最近 session id 恢复。

```powershell
tokendance resume <latest-session-id>
```

期望：传入最近 session id 时成功；传入非最近或不存在 id 时输出 `Session <id> was not found.`。

### transcript search

- [ ] 搜索刚才写入的 transcript。

```text
/transcript search e2e
```

期望：返回匹配记录的 `seq` 和 `type`；无匹配时显示 `No transcript matches.`。

### compact

- [ ] 生成 compact 摘要。

```text
/compact
```

期望：`.tokendance\sessions\<session-id>\compact\compact-0001.md` 被创建，内容包含 seq 范围和事件数量。

## 7. PowerShell 命令与质量门

- [ ] 在交互 shell 中运行只读质量命令。

```text
/quality Get-ChildItem
```

期望：返回 `exit_code: 0`，并展示 PowerShell 输出预览。

- [ ] 运行 unittest 质量命令。

```text
/quality python -m unittest tests.unit.cli.test_main
```

期望：返回 `exit_code: 0` 和测试输出；如果失败，应显示 stdout/stderr 预览。

- [ ] 验收 timeout 与输出 artifact。

期望：长输出或超时命令不会破坏 transcript；超长 stdout/stderr 写入 session 下的 `tool-outputs` artifact。

## 8. Git diff 与 review

- [ ] 在临时 Git 仓库或测试 worktree 中制造可回滚 diff。

```powershell
git status --short
```

期望：先确认当前改动来源；不要回滚他人改动。

- [ ] 修改一个测试文件或临时文件，加入一行 `TODO`。

- [ ] 在交互 shell 运行：

```text
/diff
/review
```

期望：`/diff` 显示 Git diff；`/review` 对新增 `TODO` 给出 warning；含 merge conflict marker 的 diff 给出 error。

## 9. Task 与 Todo 验收

验收前先确认 `TaskService`、`TodoService`、`task_*` 工具和 `todo_*` 工具存在，并确认默认 `CoreRuntime` registry 暴露 task/todo 工具。

- [ ] 验收 service 与工具层。

```powershell
python -m unittest tests.unit.tasks.test_task_store tests.unit.tasks.test_todo_store
python -m unittest tests.unit.tools.test_task_todo
```

期望：task 能 create/list/get/update status/add dependency/link session/link worktree；todo 能 write/update/list，并可关联 task id。

- [ ] 使用真实 provider 或定制 MockProvider tool call 做交互验收。

```text
请创建一个持久 task：标题为 Stage 15 E2E，描述为 README acceptance。
请列出当前 task。
请创建一个 todo：Run unittest，并关联刚才的 task。
请把 todo 状态改成 in_progress。
请把 task 状态改成 completed。
```

期望：`.tokendance\tasks\tasks.jsonl`、`.tokendance\tasks\task-index.json`、当前 session 的 `todos.json` 被更新；task 和 todo 状态不混淆；task 可链接 session 和 worktree。

## 10. Subagent worktree 验收

验收前先确认 `src\tokendance\agents`、`src\tokendance\tools\subagent.py`、`src\tokendance\git\worktree.py`、`subagent_run`、`worktree_create/list/keep/remove`、`/agents`、`/worktree` 是否存在，并确认默认 `CoreRuntime` registry 暴露 subagent/worktree 工具。

- [ ] 验收 subagent 与 worktree service/tool 层。

```powershell
python -m unittest tests.unit.agents.test_manager tests.unit.git.test_worktree tests.unit.tools.test_subagent
```

期望：readonly subagent 返回 summary 且无 changed files；coding subagent 在独立 worktree 中报告 changed files、diff 和 validation result；worktree remove 在 dirty changes 存在时拒绝静默删除。

- [ ] 验收 `/agents` 和 `/worktree` slash 命令。

```text
/agents
/worktree list
/worktree create stage15-wt
/worktree list
/worktree remove stage15-wt
```

期望：`/agents` 在没有记录时显示 `No subagents.`；`/worktree create` 在 Git 仓库内创建隔离 worktree；`/worktree list` 能列出它；`/worktree remove` 能在干净 worktree 上删除它。若 worktree 有未提交改动，remove 应提示需要 `--discard`，不能静默删除。

- [ ] 验收 readonly investigator。

期望：主 agent 可以启动 readonly subagent；subagent 能返回 summary；工作区无文件修改。

- [ ] 验收 readonly reviewer。

期望：reviewer 只读取 diff 或文件上下文；结果包含 findings；不修改文件。

- [ ] 验收 coding worker 的 worktree 隔离。

```powershell
git worktree list --porcelain
```

期望：修改型 subagent 默认创建独立 worktree；changed files、diff、validation result 都来自该 worktree，不直接写主工作区。

- [ ] 验收采纳与丢弃。

期望：主 agent 能展示 subagent summary、changed files、diff、validation result，并让操作者选择采纳或丢弃。

- [ ] 验收清理保护。

期望：删除 worktree 前检查未保存改动；存在 dirty changes 时拒绝静默删除或要求明确确认。

## 11. 故障排查验收

- [ ] `tokendance` 命令不可用：确认 editable install 或 pipx install 已完成，重新打开 PowerShell。
- [ ] PowerShell 激活失败：直接用 `.\.venv\Scripts\python.exe` 运行命令，不修改全局执行策略。
- [ ] 无 API key：设置 `$env:OPENAI_API_KEY` 或 `$env:ANTHROPIC_API_KEY`。
- [ ] resume 无 session：先启动一次 `tokendance` 并 `/exit`。
- [ ] Git 命令失败：确认当前目录是 Git 仓库。
- [ ] 权限被拒绝：确认目标路径在 workspace 内，且命令不是高风险 PowerShell 命令。
- [ ] task/todo 交互不可用：先确认 provider 是否发出了对应 tool call，再运行 service/tool/runtime registry 单元测试定位。
- [ ] subagent worktree 不可用：确认 Stage 13 文件、默认 runtime 工具注册与 slash 命令是否已经合并。

## 12. 最终通过标准

- [ ] README 能指导从零安装、运行 doctor、进入交互 shell、设置 API key、运行 unittest。
- [ ] Windows PowerShell 路径、环境变量、venv、pipx 和常见故障都有说明。
- [ ] `tokendance doctor` 在顶层和交互 shell 中可运行。
- [ ] 权限模式和高风险 PowerShell 命令行为有测试或手动验收步骤。
- [ ] 新会话、resume、transcript、compact、PowerShell 命令、Git diff、review 有可执行手动步骤。
- [ ] task/todo、subagent worktree 有明确验收步骤，并标注当前代码接入状态。
