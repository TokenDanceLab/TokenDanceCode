# TokenDanceCode 并行推进计划

更新时间：2026-06-09

## 稳定基线

第一波并行开发基线是 `codex/ts-refactor` 的稳定提交：

- `f49fbc0 feat(provider): add openai chat completions`
- `ce99022 feat(cli): add tokendance gateway preset`

基线验证：

- `pnpm verify`：25 个测试文件、135 个测试通过。
- `pnpm pack:check`：core/sdk/cli dry-run package 内容可生成。
- `git diff --check`：无内容错误，只有 Windows CRLF 提示。

第二波并行开发基线是 `codex/ts-refactor` 的稳定提交：

- `11692ad feat(agenthub): expose tokendanceid login helper`

第二波基线验证：

- `pnpm release:next:check`：26 个测试文件、160 个测试通过，core/sdk/cli dry-run pack 和本地 tarball install smoke 通过。
- `git diff --check`：无内容错误，只有 Windows CRLF 提示。

第三波并行开发基线是 `codex/ts-refactor` 的稳定提交：

- `d532cbd feat(doctor): surface provider readiness`

第三波基线验证：

- `pnpm release:next:check`：typecheck 通过，Vitest 26 个测试文件、179 个测试通过，core/sdk/cli dry-run pack 和本地 tarball install smoke 通过。
- built CLI temp-home smoke：`doctor --json` 在临时 `USERPROFILE/HOME` 下返回 mock provider readiness pass，未读取用户全局 `.tokendance`。
- `git diff --check`：无内容错误，只有 Windows CRLF 提示。

当前稳定基线是 `codex/ts-refactor` 的提交：

- `c70bc6f test(pack): smoke cli json contracts`

当前基线验证：

- `pnpm release:next:check`：typecheck 通过，Vitest 26 个测试文件、197 个测试通过，core/sdk/cli dry-run pack 和增强 tarball install smoke 通过。
- `pnpm pack:smoke`：临时项目安装真实 core/sdk/cli tarball 后验证 SDK import、mock turn、CLI version、`doctor --json` 的 `agentHub.ready` / `provider-ready`，以及 `quality --json` 结构化结果。
- `git diff --check`：通过。

Wave 5 稳定基线：

- `codex/ts-refactor` 已合入 W24-W33，并完成 subagent review follow-up：subagent 专属日志使用 `.tokendance/agents/<agent-id>/events.jsonl`，`AgentRunRecord.eventLogPath` 为推荐字段，避免与 session `TranscriptEnvelope` 的 `.tokendance/sessions/<session-id>/transcript.jsonl` 混淆。
- `agent.stream` schema 进入 v2，因为 envelope required fields 包含 `source_event_type`；AgentHub 侧应以 `TOKEN_DANCE_CODE_PACKAGE.agentHub.agentStreamSchemaVersion === 2` 做启动检查。
- SDK barrel 现在显式 re-export AgentHub facade 公开类型，包括 `TaskRecord`、`TodoRecord`、`ToolMetadata`、`ToolResult` 和 `WorktreeRecord`。
- Wave 5 gate：`pnpm release:next:check` 通过，包含 `contract:check`、typecheck、Vitest 26 个测试文件/240 个测试、build、core/sdk/cli dry-run pack 和 tarball install smoke。
- npm 仍未发布；当前状态是 release-owner 可审核的 next prerelease baseline，不是已发布版本。

Post-Wave 5 稳定化切片：

- 顶层 CLI command registry 已落地：`TOP_LEVEL_COMMAND_METADATA` 集中维护 top-level command 的 id/category/title/usage/aliases/json contract，help 和 handler 测试从 registry 派生；slash command registry、层级 instruction discovery、provider history normalization 留作下一波。
- 权限 renderer 改为 tool-first 行格式，便于 AgentHub 日志面板按工具名扫描 permission/tool failure/usage。
- `pack:smoke` 新增安装后 package privacy scan，跟随 pnpm scoped package symlink，并在 0 个可读包文件时失败，阻止本机路径、npm token、npm auth token 和 private key material 进入 `@tokendance/*` tarball。
- 活跃验收文档和 package metadata regression 已把 Python v0.1 旧 checklist 隔离为历史附录，TS 分支活跃 release gate 以 `pnpm release:next:check` 和 AgentHub v2 contract 为准。
- 最新 gate：`pnpm release:next:check` 通过，Vitest 26 个测试文件/242 个测试，`pnpm wave5:status -- --json` 通过，`git diff --check` 通过。

Wave 6 启动基线：

- 基线提交：`8af8ab1 chore(release): prepare first npm candidate`
- 发布状态：本地 npm first candidate 已通过 `pnpm release:next:check`，但 2026-06-09 17:02 HKT 复查 npm registry 时 `@tokendance/code-core`、`@tokendance/code-sdk`、`@tokendance/code-cli` 仍返回 `E404`。公开状态应写成“ready for release review”，不能写成“published”。
- 发布清单：`docs/release-readiness.md`
- 主线治理：英文 README 优先，root README 和 CLI package README 第一屏使用纯 ASCII `TOKENDANCE` CLI hero；公开文档不得写入本机 secret-store 路径。

## Wave 6 Workstream Matrix

Wave 6 从 `8af8ab1` 切出。目标是继续向“可真实发布、AgentHub 可直接消费、自用框架长期维护”推进，同时保持克制：不引入 daemon、IDE bridge、OpenTUI、插件市场或云端团队系统。

| Label | Branch | Worktree | 目标 | 主要文件边界 | 验收命令 |
|---|---|---|---|---|---|
| `W34-Architecture-Compare` | `codex/wave6-architecture-compare` | `.worktrees/wave6-architecture-compare` | 专门对比 ClaudeCode / Codex / OpenCode，补 Wave 6 架构判断和拒绝项，避免功能失控 | `docs/架构对标评估.md`, `docs/TS重构路线图.md`, `docs/并行推进计划.md` | `pnpm test packages/sdk/tests/package-metadata.test.ts`, `git diff --check` |
| `W35-Slash-Command-Registry` | `codex/wave6-slash-command-registry` | `.worktrees/wave6-slash-command-registry` | 为交互式 slash commands 建 metadata registry，减少 help、usage 和 handler 漂移 | `packages/cli/src/main.ts`, 可新增 `packages/cli/src/slash-commands.ts`, `packages/cli/tests/main.test.ts` | `pnpm test packages/cli/tests/main.test.ts`, `git diff --check` |
| `W36-Instruction-Discovery` | `codex/wave6-instruction-discovery` | `.worktrees/wave6-instruction-discovery` | 增强项目规则发现：从工作目录向上到 git root 发现 AGENTS/CLAUDE/.tokendance instructions，去重并限制读取范围 | `packages/core/src/context-builder.ts`, `packages/core/tests/context-builder.test.ts`, `packages/core/tests/runtime.test.ts` | `pnpm test packages/core/tests/context-builder.test.ts packages/core/tests/runtime.test.ts` |
| `W37-Provider-History` | `codex/wave6-provider-history` | `.worktrees/wave6-provider-history` | 统一多轮 provider history 和工具结果映射，避免重复或丢失 tool result | `packages/core/src/*provider*.ts`, provider tests | `pnpm test packages/core/tests/openai-provider.test.ts packages/core/tests/openai-chat-provider.test.ts packages/core/tests/anthropic-provider.test.ts` |
| `W38-SDK-Runner-Public` | `codex/wave6-sdk-runner-public` | `.worktrees/wave6-sdk-runner-public` | 将 AgentHub runner contract 从示例包提升为 SDK 稳定 facade 或让示例包变薄 | `packages/sdk/src`, `packages/sdk/tests`, `packages/agenthub-example`, `docs/agenthub-sdk.md` | `pnpm test packages/sdk/tests/sdk.test.ts packages/sdk/tests/agenthub-events.test.ts packages/agenthub-example/tests/agenthub-runner.test.ts` |
| `W39-Permission-Profiles` | `codex/wave6-permission-profiles` | `.worktrees/wave6-permission-profiles` | 完善 default/safe/auto/yolo permission metadata，让 AgentHub 可解释权限姿态 | `packages/core/src/permissions.ts`, `packages/core/src/tools.ts`, permission/tool/SDK tests | `pnpm test packages/core/tests/permissions.test.ts packages/core/tests/tools.test.ts packages/sdk/tests/sdk.test.ts` |
| `W40-CLI-TUI-Brand` | `codex/wave6-cli-tui-brand` | `.worktrees/wave6-cli-tui-brand` | 改善 scrollback-first CLI/TUI 品牌和状态输出，不引入 full-screen TUI | `packages/cli/src/renderer.ts`, `packages/cli/src/format.ts`, `packages/cli/src/main.ts`, CLI tests | `pnpm test packages/cli/tests/renderer.test.ts packages/cli/tests/main.test.ts` |

Wave 6 合并建议顺序：`W34 -> W35 -> W36 -> W37 -> W39 -> W38 -> W40`。`W35` 和 `W40` 都可能触碰 CLI help/renderer，最后由协调侧整理冲突并跑 `pnpm release:next:check`。

## 并行原则

- 每个方向使用独立 worktree 和独立 branch，避免互相覆盖。
- 每个 worktree 必须以 TDD 推进：先加失败测试，再实现，再补文档。
- 每个 worktree 的完成定义都是 `pnpm verify` 通过；涉及包内容时还要跑 `pnpm pack:check`。
- TokenDance Gateway 模型调用继续使用 TokenDance API key；TokenDanceID/OIDC 只作为 AgentHub/Hub 或管理界面的身份会话平面。
- 不发布 npm、不碰生产、不写真实 secret。npm 只做发布准备和本地 pack/install smoke，发布需要单独确认。
- TUI 可以新增 UI 包或 CLI 子命令，但不得把当前薄 CLI 变成混乱的大文件；复杂渲染组件应拆出独立模块。

## Workstream Matrix

| Label | Branch | Worktree | 目标 | 主要文件边界 | 验收命令 |
|---|---|---|---|---|---|
| `W1-CLI-TUI` | `codex/parallel-cli-tui` | `.worktrees/parallel-cli-tui` | 改善 CLI/TUI 体验：彩色 renderer、状态栏、帮助分组、TUI scaffold、错误提示和 Gateway 初次使用路径 | `packages/cli/`, 可新增 `packages/tui/`, `README.md`, `docs/端到端验收清单.md` | `pnpm test packages/cli/tests/main.test.ts packages/cli/tests/renderer.test.ts`, `pnpm verify` |
| `W2-SDK-AgentHub` | `codex/parallel-sdk-agenthub` | `.worktrees/parallel-sdk-agenthub` | 强化 AgentHub SDK contract：runner options、event envelope、doctor/packageInfo、session resume/context preview、远程审批示例 | `packages/sdk/`, `packages/agenthub-example/`, `docs/agenthub-sdk.md` | `pnpm test packages/sdk/tests/sdk.test.ts packages/agenthub-example/tests/agenthub-runner.test.ts`, `pnpm verify` |
| `W3-LLM-Providers` | `codex/parallel-llm-providers` | `.worktrees/parallel-llm-providers` | 强化 LLM 接入：provider error normalization、base URL/key precedence、真实 provider integration skip gate、OpenAI/Anthropic/Gateway 协议文档 | `packages/core/src/*provider*`, `packages/core/src/config.ts`, provider tests, `docs/端到端验收清单.md` | `pnpm test packages/core/tests/openai-provider.test.ts packages/core/tests/openai-chat-provider.test.ts packages/core/tests/anthropic-provider.test.ts packages/core/tests/config.test.ts`, `pnpm verify` |
| `W4-Safety-Permissions` | `codex/parallel-safety-permissions` | `.worktrees/parallel-safety-permissions` | 权限管理完善：permission reason、approval bridge edge cases、危险命令保护、工具风险元数据和拒绝时 transcript 证据 | `packages/core/src/permissions.ts`, `packages/core/src/tools.ts`, shell/file/git/patch tests, `packages/sdk/src/approval-bridge.ts` | `pnpm test packages/core/tests/permissions.test.ts packages/core/tests/shell-tools.test.ts packages/sdk/tests/approval-bridge.test.ts`, `pnpm verify` |
| `W5-Threads-Tasks` | `codex/parallel-threads-tasks` | `.worktrees/parallel-threads-tasks` | 线程/任务/记忆闭环：resume/compact/search、task/todo/worktree/subagent facade、session sidecar 元数据和 AgentHub 调试面板数据 | `packages/core/src/resume.ts`, `compact.ts`, `transcript.ts`, `tasks.ts`, `worktrees.ts`, `agents.ts`, SDK facade docs | `pnpm test packages/core/tests/compact-resume.test.ts packages/core/tests/tasks.test.ts packages/core/tests/worktrees.test.ts packages/core/tests/agents.test.ts packages/sdk/tests/sdk.test.ts`, `pnpm verify` |
| `W6-Brand-Release-Tests` | `codex/parallel-brand-release-tests` | `.worktrees/parallel-brand-release-tests` | 品牌/文档/npm 发布准备：package metadata、README/package README、LICENSE、npm prerelease checklist、pack/install smoke、测试矩阵说明 | package manifests, root docs, `docs/TS重构路线图.md`, `docs/端到端验收清单.md` | `pnpm verify`, `pnpm pack:check`, local tarball install smoke |

## Wave 2 Workstream Matrix

| Label | Branch | Worktree | 目标 | 主要文件边界 | 验收命令 |
|---|---|---|---|---|---|
| `W7-CLI-Onboarding` | `codex/next-cli-onboarding` | `.worktrees/next-cli-onboarding` | 增加只读首次运行/quickstart CLI 面，串起 doctor/config、provider、Gateway preset、TokenDanceID login URL helper 和验证命令 | `packages/cli/src/main.ts`, `packages/cli/tests/main.test.ts`, README quickstart 文档, `docs/端到端验收清单.md` | `pnpm test packages/cli/tests/main.test.ts`, `pnpm verify` |
| `W8-Renderer-Polish` | `codex/next-renderer-polish` | `.worktrees/next-renderer-polish` | 改善 CLI renderer/TUI 可读性：状态、权限、风险、错误和 usage 的 ANSI 高亮；保持无颜色输出稳定 | `packages/cli/src/renderer.ts`, `packages/cli/src/format.ts`, `packages/cli/tests/renderer.test.ts` | `pnpm test packages/cli/tests/renderer.test.ts`, `pnpm verify` |
| `W9-Thread-Session-SDK` | `codex/next-thread-session-mgmt` | `.worktrees/next-thread-session-mgmt` | 增强 AgentHub 可用的只读 session/thread 管理 facade，例如列出 session metadata，不改 transcript schema | `packages/core/src/resume.ts`, session/transcript helpers, `packages/sdk/src/index.ts`, SDK tests, SDK docs | `pnpm test packages/sdk/tests/sdk.test.ts packages/core/tests/compact-resume.test.ts`, `pnpm verify` |
| `W10-AgentHub-E2E-Example` | `codex/next-sdk-agenthub-e2e` | `.worktrees/next-sdk-agenthub-e2e` | 提升私有 AgentHub 示例包的可复制性，覆盖 runner 创建、事件、远程审批、package/doctor bootstrap 和 TokenDanceID login facade | `packages/agenthub-example/`, `docs/agenthub-sdk.md` | `pnpm test packages/agenthub-example/tests/agenthub-runner.test.ts`, `pnpm verify` |

## Wave 3 Workstream Matrix

Wave 3 从 `d532cbd` 切出新 worktree，不复用 Wave 1/2 旧目录。每个 workstream 必须先确认自己的 worktree HEAD 是 `d532cbd`，再开始 TDD。

| Label | Branch | Worktree | 目标 | 主要文件边界 | 验收命令 |
|---|---|---|---|---|---|
| `W11-CLI-Commands` | `codex/wave3-cli-commands` | `.worktrees/wave3-cli-commands` | 收紧 CLI 命令完整度和可读性：doctor/config/run/resume/context/quality 的错误提示、JSON 形态和帮助文案，保持薄 CLI | `packages/cli/src/main.ts`, `packages/cli/tests/main.test.ts`, `packages/cli/README.md`, root README CLI 段落 | `pnpm test packages/cli/tests/main.test.ts`, `pnpm verify` |
| `W12-SDK-AgentHub-Contract` | `codex/wave3-sdk-agenthub-contract` | `.worktrees/wave3-sdk-agenthub-contract` | 强化 SDK 给 AgentHub 的 contract：doctor readiness、package manifest、runner bootstrap、typed event/approval facade 的文档和测试闭环 | `packages/sdk/`, `packages/agenthub-example/`, `docs/agenthub-sdk.md` | `pnpm test packages/sdk/tests/sdk.test.ts packages/agenthub-example/tests/agenthub-runner.test.ts`, `pnpm verify` |
| `W13-TUI-Renderer-Polish` | `codex/wave3-tui-renderer-polish` | `.worktrees/wave3-tui-renderer-polish` | 改善 TUI/renderer 观感：颜色 token、状态/权限/风险/usage 高亮、无颜色稳定输出、紧凑但可扫描的交互文本 | `packages/cli/src/renderer.ts`, `packages/cli/src/format.ts`, renderer tests, CLI README 截图/说明 | `pnpm test packages/cli/tests/renderer.test.ts packages/cli/tests/main.test.ts`, `pnpm verify` |
| `W14-LLM-Gateway-Hardening` | `codex/wave3-llm-gateway-hardening` | `.worktrees/wave3-llm-gateway-hardening` | 完善 OpenAI/Anthropic/TokenDance Gateway 接入：key/base URL 优先级、provider validation、错误归一化、integration skip gate 文档 | `packages/core/src/*provider*`, `packages/core/src/config.ts`, provider/config tests, provider docs | `pnpm test packages/core/tests/openai-provider.test.ts packages/core/tests/openai-chat-provider.test.ts packages/core/tests/anthropic-provider.test.ts packages/core/tests/config.test.ts`, `pnpm verify` |
| `W15-Permissions-Safety` | `codex/wave3-permissions-safety` | `.worktrees/wave3-permissions-safety` | 完善权限管理：permission reason、危险 PowerShell 拒绝证据、approval bridge 边界、工具风险 metadata | `packages/core/src/permissions.ts`, `packages/core/src/powershell.ts`, `packages/core/src/tools.ts`, `packages/sdk/src/approval-bridge.ts`, related tests | `pnpm test packages/core/tests/permissions.test.ts packages/core/tests/powershell.test.ts packages/core/tests/shell-tools.test.ts packages/sdk/tests/approval-bridge.test.ts`, `pnpm verify` |
| `W16-Thread-Task-Management` | `codex/wave3-thread-task-management` | `.worktrees/wave3-thread-task-management` | 完善线程管理闭环：sessions/resume/compact/transcript search/task/todo/worktree/subagent 的 AgentHub 可消费元数据 | `packages/core/src/resume.ts`, `transcript.ts`, `compact.ts`, `tasks.ts`, `agents.ts`, `worktrees.ts`, SDK facade docs/tests | `pnpm test packages/core/tests/compact-resume.test.ts packages/core/tests/tasks.test.ts packages/core/tests/agents.test.ts packages/core/tests/worktrees.test.ts packages/sdk/tests/sdk.test.ts`, `pnpm verify` |
| `W17-Brand-Release-Tests` | `codex/wave3-brand-release-tests` | `.worktrees/wave3-brand-release-tests` | 品牌、npm 发布准备和测试矩阵：README/package README、release checklist、pack smoke、AgentHub 对接说明、残余风险清单 | package manifests, README/docs, `docs/TS重构路线图.md`, `docs/端到端验收清单.md`, package README files | `pnpm release:next:check`, `pnpm pack:smoke` |

## Wave 3 Integration Result

Wave 3 已合入 `codex/ts-refactor`，合入顺序和提交如下：

| Label | Worker Commit | Merge Commit | 结果 |
|---|---|---|---|
| `W14-LLM-Gateway-Hardening` | `d9173b7 fix(core): harden provider env and response validation` | `merge wave3 llm gateway hardening` | 已合入，provider/key/base URL/error normalization 加强 |
| `W15-Permissions-Safety` | `d9cb00c Improve permission safety evidence` | `merge wave3 permissions safety` | 已合入，permission reason、PowerShell evidence、tool metadata 加强 |
| `W16-Thread-Task-Management` | `d147640 feat(agenthub): close thread task metadata APIs` | `merge wave3 thread task management` | 已合入，session transcript search、worktree dirty、agent task metadata 加强 |
| `W12-SDK-AgentHub-Contract` | `f949ea0 Strengthen AgentHub SDK contract readiness` | `merge wave3 sdk agenthub contract` | 已合入，doctor AgentHub readiness、runner bootstrap、manifest features 加强 |
| `W13-TUI-Renderer-Polish` | `f29cc20 polish cli renderer permission output` | `merge wave3 tui renderer polish` | 已合入，permission 输出压缩和 no-color/color renderer 测试加强 |
| `W11-CLI-Commands` | `29b0824 Tighten CLI doctor and quality output` | `merge wave3 cli commands` | 已合入，doctor 参数校验和 `quality --json` 加强 |
| `W17-Brand-Release-Tests` | `fc5a492 docs: strengthen next release readiness` | `merge wave3 brand release tests` | 已合入，npm next checklist、README/package README、metadata regression 加强 |

Wave 3 主线验证：

- Focused suite：17 个测试文件、160 个测试通过，覆盖 provider/config、permissions、threads/tasks、SDK/AgentHub、metadata、CLI/renderer。
- `pnpm release:next:check`：typecheck 通过，Vitest 26 个测试文件、197 个测试通过，core/sdk/cli dry-run pack 和 tarball install smoke 通过。
- built CLI temp-home smoke：`doctor --json` 在临时 `USERPROFILE/HOME` 下返回 mock provider readiness 和 `agentHub.ready=true`；`quality --json "Write-Output ok"` 返回结构化 `passed/result`。
- `git diff --check`：通过。

## Wave 4 Workstream Matrix

Wave 4 从 `82096a6` 切出新 worktree。重点不是继续堆大功能，而是把稳定基线打磨到 AgentHub 可以长期消费：CLI 入口降复杂度、SDK 消费示例更接近 AgentHub、滚动 TUI 更可读、真实 provider smoke 有安全 opt-in、权限策略可审计、线程生命周期更完整。每个 worker 必须先确认 `git rev-parse --short HEAD` 为 `82096a6`，再开始 TDD。

| Label | Branch | Worktree | 目标 | 主要文件边界 | 验收命令 |
|---|---|---|---|---|---|
| `W18-CLI-Command-Architecture` | `codex/wave4-cli-command-architecture` | `.worktrees/wave4-cli-command-architecture` | 降低 `packages/cli/src/main.ts` 复杂度：抽出命令解析/执行 helper，保持 `tokendance` 薄入口，现有命令 JSON 和 usage 行为不回退 | `packages/cli/src/main.ts`, 可新增 `packages/cli/src/commands.ts`, `packages/cli/tests/main.test.ts`, CLI README | `pnpm test packages/cli/tests/main.test.ts`, `pnpm verify` |
| `W19-SDK-AgentHub-Consumer-Fixture` | `codex/wave4-sdk-agenthub-consumer-fixture` | `.worktrees/wave4-sdk-agenthub-consumer-fixture` | 提供更接近 AgentHub 的消费侧 fixture：启动检查、resume-or-start、event sink、approval bridge、TokenDanceID login facade 和 package manifest 一条链路 | `packages/agenthub-example/`, `packages/sdk/`, `docs/agenthub-sdk.md` | `pnpm test packages/sdk/tests/sdk.test.ts packages/sdk/tests/agenthub-events.test.ts packages/agenthub-example/tests/agenthub-runner.test.ts`, `pnpm verify` |
| `W20-TUI-Interaction-Polish` | `codex/wave4-tui-interaction-polish` | `.worktrees/wave4-tui-interaction-polish` | 打磨滚动式 TUI：状态/权限/风险/usage 高亮、错误块、无颜色稳定快照，不引入全屏复杂 TUI | `packages/cli/src/renderer.ts`, `packages/cli/src/format.ts`, `packages/cli/tests/renderer.test.ts`, CLI README | `pnpm test packages/cli/tests/renderer.test.ts packages/cli/tests/main.test.ts`, `pnpm verify` |
| `W21-LLM-Real-Smoke-Gates` | `codex/wave4-llm-real-smoke-gates` | `.worktrees/wave4-llm-real-smoke-gates` | 增加真实 provider/Gateway smoke 的显式 opt-in 门和文档：默认不读项目 `.env`、不使用真实 key，支持 OpenAI/Anthropic/Gateway 配置预检 | `packages/core/src/*provider*`, `packages/core/src/config.ts`, provider/config tests, docs | `pnpm test packages/core/tests/openai-provider.test.ts packages/core/tests/openai-chat-provider.test.ts packages/core/tests/anthropic-provider.test.ts packages/core/tests/config.test.ts`, `pnpm verify` |
| `W22-Permission-Policy-Audit` | `codex/wave4-permission-policy-audit` | `.worktrees/wave4-permission-policy-audit` | 权限策略审计强化：拒绝原因、风险 metadata、PowerShell 高危命令证据、approval bridge 超时/重复决策边界 | `packages/core/src/permissions.ts`, `packages/core/src/powershell.ts`, `packages/core/src/tools.ts`, `packages/sdk/src/approval-bridge.ts`, related tests | `pnpm test packages/core/tests/permissions.test.ts packages/core/tests/powershell.test.ts packages/core/tests/shell-tools.test.ts packages/sdk/tests/approval-bridge.test.ts`, `pnpm verify` |
| `W23-Thread-Session-Lifecycle` | `codex/wave4-thread-session-lifecycle` | `.worktrees/wave4-thread-session-lifecycle` | 线程生命周期完善：session list/search/export/prune 候选、resume 错误诊断、AgentHub 调试面板需要的只读元数据，避免改 transcript schema | `packages/core/src/resume.ts`, `packages/core/src/transcript.ts`, `packages/sdk/src/index.ts`, SDK/core tests, docs | `pnpm test packages/core/tests/compact-resume.test.ts packages/sdk/tests/sdk.test.ts`, `pnpm verify` |

Wave 4 主线验证：

- `pnpm verify`：typecheck 通过，Vitest 26 个测试文件、213 个测试通过。
- `pnpm release:next:check`：typecheck、Vitest 26 个测试文件 213 个测试、build、core/sdk/cli dry-run pack、增强 tarball install smoke 通过。
- `pnpm wave4:status -- --json`：6 个 worker worktree 均 clean，分支正确，且 `82096a6` 是各自 HEAD 的祖先。
- Focused suites 覆盖 CLI command/renderer、SDK AgentHub fixture、provider smoke gate、permission audit、session lifecycle metadata。

## Wave 5 Workstream Matrix

Wave 5 从 `0f631f3 docs(parallel): record wave4 integration` 切出新 worktree。Wave 5 的目标是把“稳定可消费基线”推进为“可长期维护的自用 Agent 框架基线”：参考 ClaudeCode / Codex / OpenCode 的架构思路，但只吸收薄 CLI、结构化事件、权限审计、session/transcript、provider protocol、SDK contract 和可测试发布门禁，不复制云端平台、IDE bridge、插件市场、复杂 daemon 或企业级 team workflow。

W24-Reference-Architecture 的文档闭环必须把以下决策落成可测试文本：CLI main.ts 过大风险进入 command registry lane；Codex contract/schema drift gate 保护 SDK manifest、AgentHub event envelope、transcript schema、provider schema 和 CLI JSON 输出；OpenCode command metadata registry 只作为 id/category/title/aliases/usage/JSON contract 参考；明确拒绝 app-server daemon、拒绝 OpenTUI、拒绝 plugin marketplace、拒绝 native installer。

协调侧新增只读状态检查：

```powershell
pnpm wave5:status -- --json
pnpm wave5:status -- --strict-clean
```

`pnpm wave5:status` 只检查 worktree 路径、分支、`0f631f3` 是否为 HEAD 祖先、dirty 状态；它不发布 npm、不访问 provider、不读取 secrets。

| Label | Branch | Worktree | 目标 | 主要文件边界 | 验收命令 |
|---|---|---|---|---|---|
| `W24-Reference-Architecture` | `codex/wave5-reference-architecture` | `.worktrees/wave5-reference-architecture` | 汇总 ClaudeCode / Codex / OpenCode 对标结果，形成保留/拒绝/延后决策，重点固化 command registry lane、contract/schema drift gate 和非目标清单，防止盲目堆功能 | `docs/架构对标评估.md`, `docs/TS重构路线图.md`, `docs/并行推进计划.md` | `pnpm test packages/sdk/tests/package-metadata.test.ts`, `pnpm verify` |
| `W25-Release-NPM-Baseline` | `codex/wave5-release-npm-baseline` | `.worktrees/wave5-release-npm-baseline` | 发布基线再加固：package metadata、tarball smoke、npm next 人工门禁和 AgentHub 可消费证据；不执行 publish | `package.json`, `packages/*/package.json`, `scripts/smoke-tarball-install.mjs`, `packages/sdk/tests/package-metadata.test.ts`, README/package README | `pnpm test packages/sdk/tests/package-metadata.test.ts`, `pnpm release:next:check` |
| `W26-SDK-AgentHub-Contract` | `codex/wave5-sdk-agenthub-contract` | `.worktrees/wave5-sdk-agenthub-contract` | 强化 SDK contract：manifest feature flags、typed event envelope、doctor readiness、approval bridge 边界 | `packages/sdk/src/`, `packages/sdk/tests/`, `docs/agenthub-sdk.md` | `pnpm test packages/sdk/tests/sdk.test.ts packages/sdk/tests/agenthub-events.test.ts packages/sdk/tests/approval-bridge.test.ts` |
| `W27-AgentHub-Consumer-Fixture` | `codex/wave5-agenthub-consumer-fixture` | `.worktrees/wave5-agenthub-consumer-fixture` | 提升私有 AgentHub 示例可复制性：startup、resume、event sink、approval、OIDC login facade 一条链路 | `packages/agenthub-example/`, `docs/agenthub-sdk.md` | `pnpm test packages/agenthub-example/tests/agenthub-runner.test.ts`, `pnpm verify` |
| `W28-Gateway-Quickstart` | `codex/wave5-gateway-quickstart` | `.worktrees/wave5-gateway-quickstart` | 打磨一键 TokenDance Gateway preset 和 config/doctor 体验，继续保持“不生成 key、不覆盖 key、不泄露 key” | `packages/core/src/config.ts`, `packages/core/tests/config.test.ts`, `packages/cli/src/main.ts`, `packages/cli/tests/main.test.ts`, README | `pnpm test packages/core/tests/config.test.ts packages/cli/tests/main.test.ts` |
| `W29-TokenDanceID-OIDC` | `codex/wave5-tokendanceid-oidc` | `.worktrees/wave5-tokendanceid-oidc` | 打磨 TokenDanceID OIDC 启动 helper：PKCE/state/callback 诊断、CLI JSON 输出、AgentHub Hub exchange 边界文档 | `packages/sdk/src/tokendance-id.ts`, `packages/sdk/tests/tokendance-id.test.ts`, `packages/cli/src/main.ts`, OIDC docs | `pnpm test packages/sdk/tests/tokendance-id.test.ts packages/cli/tests/main.test.ts` |
| `W30-Provider-Hardening` | `codex/wave5-provider-hardening` | `.worktrees/wave5-provider-hardening` | 加固 OpenAI Responses、OpenAI-compatible Chat/Gateway、Anthropic Messages 协议归一化、错误诊断和 opt-in smoke | `packages/core/src/*provider*.ts`, `packages/core/src/provider-errors.ts`, provider tests | `pnpm test packages/core/tests/openai-provider.test.ts packages/core/tests/openai-chat-provider.test.ts packages/core/tests/anthropic-provider.test.ts` |
| `W31-Permission-Safety` | `codex/wave5-permission-safety` | `.worktrees/wave5-permission-safety` | 加固权限和 PowerShell 安全：hard deny evidence、risk metadata、approval 不可绕过、默认/safe/auto/yolo 可回放 | `packages/core/src/permissions.ts`, `packages/core/src/powershell.ts`, `packages/core/src/shell-tools.ts`, `packages/core/src/tools.ts`, related tests | `pnpm test packages/core/tests/permissions.test.ts packages/core/tests/powershell.test.ts packages/core/tests/shell-tools.test.ts packages/core/tests/runtime.test.ts` |
| `W32-CLI-TUI-Polish` | `codex/wave5-cli-tui-polish` | `.worktrees/wave5-cli-tui-polish` | 继续打磨 scrollback-first TUI：颜色、风险/权限/usage 高亮、错误块、帮助分组；不引入 full-screen 重型 TUI | `packages/cli/src/renderer.ts`, `packages/cli/src/format.ts`, `packages/cli/tests/renderer.test.ts`, CLI README | `pnpm test packages/cli/tests/renderer.test.ts packages/cli/tests/main.test.ts` |
| `W33-Session-Subagent` | `codex/wave5-session-subagent` | `.worktrees/wave5-session-subagent` | 完善 session/resume/context/task/worktree/subagent 只读元数据和 dirty 保护，支撑 AgentHub 调试面板 | `packages/core/src/{resume,transcript,context-builder,agents,worktrees,tasks}.ts`, `packages/sdk/src/index.ts`, related tests | `pnpm test packages/core/tests/compact-resume.test.ts packages/core/tests/agents.test.ts packages/core/tests/worktrees.test.ts packages/sdk/tests/sdk.test.ts` |

Wave 5 合并建议顺序：`W24 -> W25 -> W30 -> W31 -> W33 -> W26 -> W27 -> W28 -> W29 -> W32`。`W28/W29/W32` 都可能触碰 `packages/cli/src/main.ts`，合并时由协调侧做最终冲突整理。每个 worker 必须使用 TDD、提交 scoped commit，并报告 changed files、focused tests、残余风险和 merge notes。

## Wave 6 Workstream Matrix

Wave 6 从 `8af8ab1` 切出文档对标 worktree。目标是复查 ClaudeCode / Codex / OpenCode 的 CLI agent 架构目录，把 Wave 5 的方向写成更明确的保留、拒绝、延后边界。Wave 6 不改公开 README，不发布 npm，不读取 secrets，不改其他 worktree。

本轮架构判断：

- 保留：薄 CLI、core runtime、SDK facade、provider adapter、tool registry + permission、JSONL transcript、AgentHub event mapper、本地 release/package gate。
- 拒绝：app-server daemon、remote-control 平台、plugin marketplace、OpenTUI/full-screen provider tree、desktop/IDE/browser bridge、企业/云任务/共享控制面、真实 provider key 自动发现。
- 延后：slash command registry、层级 instruction discovery、provider history normalization、更强 Windows sandbox、session export/prune UI。

| Label | Branch | Worktree | 目标 | 主要文件边界 | 验收命令 |
|---|---|---|---|---|---|
| `W34-Architecture-Compare` | `codex/wave6-architecture-compare` | `.worktrees\ts-refactor\.worktrees\wave6-architecture-compare` | 只读对比 ClaudeCode / Codex / OpenCode CLI agent 架构目录，更新 Wave 6 架构判断，防止把参考仓库的产品外壳搬进 TokenDanceCode | `docs/架构对标评估.md`, `docs/TS重构路线图.md`, `docs/并行推进计划.md` | `pnpm test packages/sdk/tests/package-metadata.test.ts`, `git diff --check` |

Wave 6 合并注意事项：

- 合并侧只需要检查上述三份 docs 文件，不应要求 README 同步。
- 如果后续 worker 想新增 command、provider、tool、session 或 AgentHub contract 行为，先把它归到保留/拒绝/延后表；归不进去则默认不做。
- `pnpm release:next:check` 仍是发布前门禁，但 W34 是 docs-only 切片，focused metadata test 和 diff hygiene 足够覆盖本次改动。

## GPT-5.5 High Agent Prompts

每个并行 agent 使用同样的硬约束：

```text
You are working in the assigned TokenDanceCode worktree only.
Base branch is codex/ts-refactor at the documented baseline.
Do not modify sibling worktrees. Do not publish npm. Do not touch production or secrets.
Use TDD: add failing tests first, observe failure, implement, then update docs.
Keep changes scoped to the assigned workstream. Run the listed focused tests and pnpm verify.
Commit only your worktree's scoped changes with a clear message.
Report: changed files, tests, residual risks, and merge notes.
```

Workstream-specific prompts should include the matching row from the matrix, the relevant files, the baseline commit, and the acceptance commands.

## Merge Strategy

1. Review each worktree independently: `git status`, focused tests, `pnpm verify`, and diff review.
2. Merge in this order to reduce conflicts:
   - Wave 3: `W14 -> W15 -> W16 -> W12 -> W13 -> W11 -> W17`
   - `W3-LLM-Providers`
   - `W4-Safety-Permissions`
   - `W5-Threads-Tasks`
   - `W2-SDK-AgentHub`
   - `W1-CLI-TUI`
   - `W6-Brand-Release-Tests`
3. After each merge, run `pnpm verify`.
4. Before npm prerelease, run `pnpm pack:check`, local tarball install smoke, and package metadata review.
