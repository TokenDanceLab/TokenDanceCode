# TokenDanceCode 并行执行状态

更新时间：2026-06-09

## Coordinator Baseline

- 协调分支：`codex/ts-refactor`
- 稳定基线：`9026166 docs: plan parallel tokendancecode workstreams`
- Gateway preset 基线：`ce99022 feat(cli): add tokendance gateway preset`
- Chat Completions provider 基线：`f49fbc0 feat(provider): add openai chat completions`
- 并行合并后基线：`17e448c merge: integrate release packaging gates`
- 最终 gate：`pnpm release:next:check` 通过，包含 `pnpm verify`、`pnpm pack:check`、`pnpm pack:smoke`；25 个测试文件、154 个测试通过。
- Diff hygiene：`git diff --check` 通过。
- OIDC/AgentHub runner 基线：`11692ad feat(agenthub): expose tokendanceid login helper`
- 当前稳定 gate：`pnpm release:next:check` 通过，包含 `pnpm verify`、`pnpm pack:check`、`pnpm pack:smoke`；26 个测试文件、160 个测试通过。
- 当前 diff hygiene：`git diff --check` 通过。
- Wave 2 首个合并基线：`0856405 merge: integrate renderer polish`
- Wave 2 当前 gate：`pnpm release:next:check` 通过，包含 `pnpm verify`、`pnpm pack:check`、`pnpm pack:smoke`；26 个测试文件、162 个测试通过。
- Wave 2 当前 diff hygiene：`git diff --check` 通过。
- Wave 2 session listing 基线：`9b0b905 merge: integrate sdk session listing`
- Wave 2 当前 gate：`pnpm release:next:check` 通过，包含 `pnpm verify`、`pnpm pack:check`、`pnpm pack:smoke`；26 个测试文件、164 个测试通过。
- Wave 2 当前 diff hygiene：`git diff --check` 通过。
- Wave 2 quickstart 基线：`7964acb merge: integrate cli quickstart onboarding`
- Wave 2 当前 gate：`pnpm release:next:check` 通过，包含 `pnpm verify`、`pnpm pack:check`、`pnpm pack:smoke`；26 个测试文件、165 个测试通过。
- Wave 2 当前 diff hygiene：`git diff --check` 通过。
- Wave 2 AgentHub fixture 基线：`4f85eb3 merge: integrate agenthub e2e fixture` + `42dd090 fix(agenthub): guard fixture oidc login inputs`
- Wave 2 当前 gate：`pnpm release:next:check` 通过，包含 `pnpm verify`、`pnpm pack:check`、`pnpm pack:smoke`；26 个测试文件、167 个测试通过。
- Wave 2 当前 diff hygiene：`git diff --check` 通过。

## Worker Results

| Label | Agent | Agent ID | Branch | Worktree | Result | Merge Commit |
|---|---|---|---|---|---|---|
| `W3-LLM-Providers` | Halley | `019eaa27-d7bc-79a3-b19f-f155ab9f8489` | `codex/parallel-llm-providers` | `.worktrees\parallel-llm-providers` | committed `3f5a84e`; focused provider/config gate passed | `a9b9e19` |
| `W4-Safety-Permissions` | Ampere | `019eaa27-d854-7d01-b261-96b1a176af98` | `codex/parallel-safety-permissions` | `.worktrees\parallel-safety-permissions` | committed `5e7ec62`; focused permission/tool/approval gate passed | `e4f88bc` |
| `W5-Threads-Tasks` | Volta / Epicurus / coordinator | `019eaa27-d913-73b1-acaf-9562b266f02b`, `019eaa2a-7bff-70c0-ab04-d8c817ff054b` | `codex/parallel-threads-tasks` | `.worktrees\parallel-threads-tasks` | subagents hit usage limits; coordinator committed `bb5f059`; focused task/CLI/SDK gate passed | `c261edf` |
| `W2-SDK-AgentHub` | Boyle | `019eaa27-d731-71e2-8683-a83f386925e2` | `codex/parallel-sdk-agenthub` | `.worktrees\parallel-sdk-agenthub` | committed `e3ba094`; focused SDK/example/metadata gate passed | `ff94b6c` |
| `W1-CLI-TUI` | Arendt | `019eaa27-d56d-7360-a518-724546696d4a` | `codex/parallel-cli-tui` | `.worktrees\parallel-cli-tui` | committed `1159c83`; CLI/renderer gate passed after conflict resolution | `9329e1e` |
| `W6-Brand-Release-Tests` | Pascal | `019eaa27-daec-7cf3-a427-15550de795f4` | `codex/parallel-brand-release-tests` | `.worktrees\parallel-brand-release-tests` | committed `09164e6`; package metadata gate passed and final release gate passed | `17e448c` |

## Merge Notes

- Merge order followed the plan: W3, W4, W5, W2, W1, W6.
- Manual conflicts were resolved in `packages/cli/src/main.ts`, `docs/端到端验收清单.md`, `README.md`, and `docs/agenthub-sdk.md`.
- `W1` help grouping was retained while preserving `W5` task `link-session` / `link-worktree` commands.
- `W2` AgentHub contract manifest was retained while adding `W6` package smoke and prerelease checks.
- npm publish remains explicitly out of scope; current state is prerelease-ready only after human confirmation.
- TokenDanceID/OIDC remains identity/session flow; TokenDance Gateway model calls remain TokenDance API key flow.

## Wave 2 Active Workers

| Label | Agent | Agent ID | Branch | Worktree | Ownership | Status |
|---|---|---|---|---|---|---|
| `W7-CLI-Onboarding` | Dewey | `019eaa5e-a5c8-7a40-afae-05d8c619c7ce` | `codex/next-cli-onboarding` | `.worktrees\next-cli-onboarding` | CLI quickstart command/REPL alias, main CLI tests, README/acceptance docs | merged `7964acb` |
| `W8-Renderer-Polish` | Noether | `019eaa5e-a67d-7282-aca1-bb667eaf0b0d` | `codex/next-renderer-polish` | `.worktrees\next-renderer-polish` | renderer/format output and renderer tests | merged `0856405` |
| `W9-Thread-Session-SDK` | Nietzsche | `019eaa5e-a74c-72b0-8af2-f089e3ebcd24` | `codex/next-thread-session-mgmt` | `.worktrees\next-thread-session-mgmt` | read-only session/thread SDK facade, core/session helpers, SDK docs/tests | merged `9b0b905` |
| `W10-AgentHub-E2E-Example` | Avicenna | `019eaa5e-f202-73e3-8936-2d576cc1b94b` | `codex/next-sdk-agenthub-e2e` | `.worktrees\next-sdk-agenthub-e2e` | private AgentHub example package copyability and e2e fixture/docs | merged `4f85eb3`; follow-up `42dd090` |

Wave 2 remained non-publish, non-production, and secret-free. All four workstreams have scoped commits, focused tests, coordinator diff review, and a fresh `pnpm release:next:check` on `codex/ts-refactor`.

## Wave 3 Integrated Baseline

- Wave 3 已合入主线并完成后续 pack smoke contract 加固。
- 当前稳定提交：`c70bc6f test(pack): smoke cli json contracts`
- 当前 gate：`pnpm release:next:check` 通过，包含 typecheck、Vitest 26 个测试文件、197 个测试、core/sdk/cli dry-run pack 和增强 tarball install smoke。
- Pack smoke contract：真实 tarball 临时安装后验证 SDK import、mock turn、CLI version、`doctor --json` 的 `agentHub.ready` / `provider-ready`，以及 `quality --json` 结构化结果。
- 当前 diff hygiene：`git diff --check` 通过。

## Wave 4 Planned Workers

Wave 4 以 `82096a6 docs(parallel): plan wave4 stabilization` 为协调基线。启动前要求每个 worker 在自己的 worktree 中确认 HEAD 为 `82096a6`，并遵守不发布 npm、不触碰生产、不写真实 secret、TDD 和 focused verification 的硬约束。
协调侧可运行 `pnpm wave4:status` 或 `pnpm wave4:status -- --json` 只读检查 6 个 worker worktree 的 branch、HEAD 和 dirty 状态。

| Label | Branch | Worktree | Scope | Status |
|---|---|---|---|---|
| `W18-CLI-Command-Architecture` | `codex/wave4-cli-command-architecture` | `.worktrees\wave4-cli-command-architecture` | CLI command architecture | merged worker commit `0f4831d`; Linnaeus `019eab40-f167-7081-b94c-ac6cc215cf78` |
| `W19-SDK-AgentHub-Consumer-Fixture` | `codex/wave4-sdk-agenthub-consumer-fixture` | `.worktrees\wave4-sdk-agenthub-consumer-fixture` | SDK / AgentHub example | merged worker commit `8f320f1`; Newton `019eab40-f20a-7e91-8b1a-21e3f7861ff4` |
| `W20-TUI-Interaction-Polish` | `codex/wave4-tui-interaction-polish` | `.worktrees\wave4-tui-interaction-polish` | CLI renderer / rolling TUI | merged worker commit `171205b`; Dirac `019eab40-f2ac-7fe2-9fee-1ef388eca129` |
| `W21-LLM-Real-Smoke-Gates` | `codex/wave4-llm-real-smoke-gates` | `.worktrees\wave4-llm-real-smoke-gates` | Provider / Gateway smoke gates | merged worker commit `2d4e542`; Planck `019eab40-f35d-7813-b4f3-ac7617f67122` |
| `W22-Permission-Policy-Audit` | `codex/wave4-permission-policy-audit` | `.worktrees\wave4-permission-policy-audit` | Permission / safety policy | merged worker commit `b5dc81a`; Parfit `019eab40-f408-7022-997c-2b69d85195bb` |
| `W23-Thread-Session-Lifecycle` | `codex/wave4-thread-session-lifecycle` | `.worktrees\wave4-thread-session-lifecycle` | Resume / session lifecycle | merged worker commit `ea0bc62`; Aristotle `019eab40-f5f7-7a33-b8d8-51e56a6ae2b5` |

## Wave 4 Integration Result

Wave 4 已全部合入 `codex/ts-refactor`，合入顺序和提交如下：

| Label | Worker Commit | Merge Commit | 结果 |
|---|---|---|---|
| `W20-TUI-Interaction-Polish` | `171205b feat(cli): polish scrollback renderer` | `cc2067c merge wave4 tui interaction polish` | 已合入，scrollback badge、颜色/无颜色 renderer 和 usage 输出加强 |
| `W19-SDK-AgentHub-Consumer-Fixture` | `8f320f1 test(agenthub): add consumer fixture chain` | `30e8ca7 merge wave4 sdk agenthub consumer fixture` | 已合入，AgentHub consumer fixture 和 manifest feature 加强 |
| `W21-LLM-Real-Smoke-Gates` | `2d4e542 test(core): gate real provider smoke` | `15c9c82 merge wave4 llm real smoke gates` | 已合入，真实 provider/Gateway smoke 显式 opt-in gate 加强 |
| `W18-CLI-Command-Architecture` | `0f4831d refactor cli command dispatch` | `066530e merge wave4 cli command architecture` | 已合入，CLI 顶层 command dispatch helper 抽出 |
| `W23-Thread-Session-Lifecycle` | `ea0bc62 feat(sdk): expose session lifecycle metadata` | `ee9afdd merge wave4 thread session lifecycle` | 已合入，session export/prune candidates/diagnostics 加强 |
| `W22-Permission-Policy-Audit` | `b5dc81a Strengthen permission policy audit` | `1f4efc2 merge wave4 permission policy audit` | 已合入，permission risk metadata、PowerShell evidence、approval timeout 加强 |

Wave 4 focused verification：

- CLI focused suite：`pnpm test packages/cli/tests/main.test.ts packages/cli/tests/renderer.test.ts` 通过，2 个测试文件、49 个测试。
- SDK/AgentHub focused suite：`pnpm test packages/sdk/tests/sdk.test.ts packages/sdk/tests/agenthub-events.test.ts packages/agenthub-example/tests/agenthub-runner.test.ts packages/sdk/tests/package-metadata.test.ts` 通过，4 个测试文件、55 个测试。
- Provider focused suite：`pnpm test packages/core/tests/openai-provider.test.ts packages/core/tests/openai-chat-provider.test.ts packages/core/tests/anthropic-provider.test.ts packages/core/tests/config.test.ts` 通过，4 个测试文件、33 个测试。
- Permission focused suite：`pnpm test packages/core/tests/permissions.test.ts packages/core/tests/powershell.test.ts packages/core/tests/shell-tools.test.ts packages/sdk/tests/approval-bridge.test.ts` 通过，4 个测试文件、22 个测试。
- Session lifecycle focused suite：`pnpm test packages/core/tests/compact-resume.test.ts packages/sdk/tests/sdk.test.ts packages/sdk/tests/package-metadata.test.ts` 通过，3 个测试文件、49 个测试。
- `pnpm verify`：typecheck 通过，Vitest 26 个测试文件、213 个测试通过。
- `pnpm release:next:check`：typecheck、Vitest 26 个测试文件 213 个测试、build、core/sdk/cli dry-run pack、增强 tarball install smoke 通过。
- `pnpm wave4:status -- --json`：6 个 worker worktree 均 clean，分支正确，且 `82096a6` 是各自 HEAD 的祖先。

## Wave 5 Planning Baseline

- Wave 5 协调基线：`0f631f3 docs(parallel): record wave4 integration`
- 当前策略：先做对标评估和可验证 worktree 调度资产，再启动并行实现；不新建 Codex 对话线程，使用 multiagent subagent 和 repo-local `.worktrees/`。
- 新增只读检查：`pnpm wave5:status` / `pnpm wave5:status -- --json` / `pnpm wave5:status -- --strict-clean`。
- 参考审计任务：ClaudeCode、Codex、OpenCode、TokenDanceCode 当前差距各一个 explorer subagent。当前差距审计已确认：npm 仍是本地 gate + 人工发布；AgentHub SDK 可消费但生产侧需接入自己的 event bus/approval/session；Gateway quickstart 是 preset，不生成 API key；TokenDanceID OIDC helper 只启动 PKCE 登录，不做 token exchange。

| Label | Branch | Worktree | Scope | Status |
|---|---|---|---|---|
| `W24-Reference-Architecture` | `codex/wave5-reference-architecture` | `.worktrees\wave5-reference-architecture` | reference comparison and architecture decisions | planned |
| `W25-Release-NPM-Baseline` | `codex/wave5-release-npm-baseline` | `.worktrees\wave5-release-npm-baseline` | release / npm baseline | planned |
| `W26-SDK-AgentHub-Contract` | `codex/wave5-sdk-agenthub-contract` | `.worktrees\wave5-sdk-agenthub-contract` | SDK AgentHub contract | planned |
| `W27-AgentHub-Consumer-Fixture` | `codex/wave5-agenthub-consumer-fixture` | `.worktrees\wave5-agenthub-consumer-fixture` | AgentHub consumer fixture | planned |
| `W28-Gateway-Quickstart` | `codex/wave5-gateway-quickstart` | `.worktrees\wave5-gateway-quickstart` | Gateway quickstart/config | planned |
| `W29-TokenDanceID-OIDC` | `codex/wave5-tokendanceid-oidc` | `.worktrees\wave5-tokendanceid-oidc` | TokenDanceID OIDC helper | planned |
| `W30-Provider-Hardening` | `codex/wave5-provider-hardening` | `.worktrees\wave5-provider-hardening` | provider protocol hardening | planned |
| `W31-Permission-Safety` | `codex/wave5-permission-safety` | `.worktrees\wave5-permission-safety` | permission / shell safety | planned |
| `W32-CLI-TUI-Polish` | `codex/wave5-cli-tui-polish` | `.worktrees\wave5-cli-tui-polish` | CLI renderer / TUI polish | planned |
| `W33-Session-Subagent` | `codex/wave5-session-subagent` | `.worktrees\wave5-session-subagent` | session / subagent lifecycle | planned |

## Wave 5 Integration Result

Wave 5 已全部合入 `codex/ts-refactor`。本轮没有发布 npm、没有触碰生产、没有写入真实 secret；所有 Gateway/OIDC 改动都停留在本地配置、SDK helper、doctor/CLI 诊断和文档边界。

| Label | Worker Commit | Merge Commit / Follow-up | 结果 |
|---|---|---|---|
| `W24-Reference-Architecture` | `9946ef9` | `merge wave5 reference architecture` | 已合入 ClaudeCode / Codex / OpenCode 对标评估，明确 command registry、contract/schema drift gate 和不复制复杂平台能力的非目标 |
| `W25-Release-NPM-Baseline` | `a7c1b5d` | `c72bf7c merge wave5 release npm baseline` | 已合入 npm next 本地门禁、`contract:check` 和 release owner 人工发布边界 |
| `W26-SDK-AgentHub-Contract` | `9b4c98d` | `merge wave5 sdk agenthub contract` | 已合入 AgentHub feature flags、typed event envelope、doctor readiness、approval bridge contract |
| `W27-AgentHub-Consumer-Fixture` | `68132ff` | `merge wave5 agenthub consumer fixture` | 已合入 AgentHub consumer fixture startup 默认工作目录和复制性测试 |
| `W28-Gateway-Quickstart` | `35da6a8` | `merge wave5 gateway quickstart` | 已合入 TokenDance Gateway preset/doctor/config 诊断；只做 key 注入和 readiness，不生成 API key |
| `W29-TokenDanceID-OIDC` | `9d0d288` | `merge wave5 tokendanceid oidc` | 已合入 TokenDanceID login URL、PKCE/state/callback 诊断；不做 token exchange/storage，Hub 继续拥有 exchange/JWKS/session |
| `W30-Provider-Hardening` | `24b3977` | `merge wave5 provider hardening` | 已合入 OpenAI Responses、OpenAI-compatible Chat/Gateway、Anthropic Messages 的错误归一化和空 key 防护 |
| `W31-Permission-Safety` | `aa0cab1` | `merge wave5 permission safety` | 已合入 approval 不可绕过 hard deny、PowerShell hard-deny evidence 和 approval scope metadata |
| `W32-CLI-TUI-Polish` | `357d79b` | `merge wave5 cli tui polish` | 已合入 scrollback renderer 的 tool summary、output metadata 和 failure block 打磨，不引入 full-screen TUI |
| `W33-Session-Subagent` | `519ebac` | `b590271 merge wave5 session subagent metadata` + follow-up review fix | 已合入 session/resume/context/task/worktree/subagent 只读元数据和 dirty 保护；follow-up 将 subagent log 改为 `events.jsonl`，`AgentRunRecord.eventLogPath` 成为推荐字段，`transcriptPath` 只保留兼容别名 |

Wave 5 focused verification：

- W33 focused suite：`pnpm test packages/core/tests/compact-resume.test.ts packages/core/tests/agents.test.ts packages/core/tests/worktrees.test.ts packages/core/tests/tasks.test.ts packages/sdk/tests/sdk.test.ts` 通过，5 个测试文件、58 个测试。
- CLI focused suite：`pnpm test packages/cli/tests/main.test.ts packages/cli/tests/renderer.test.ts` 通过，2 个测试文件、50 个测试。
- SDK/AgentHub/OIDC focused suite：`pnpm test packages/sdk/tests/sdk.test.ts packages/sdk/tests/agenthub-events.test.ts packages/sdk/tests/approval-bridge.test.ts packages/sdk/tests/package-metadata.test.ts packages/sdk/tests/tokendance-id.test.ts packages/agenthub-example/tests/agenthub-runner.test.ts` 通过，6 个测试文件、72 个测试。
- Provider/permission/runtime focused suite：`pnpm test packages/core/tests/openai-provider.test.ts packages/core/tests/openai-chat-provider.test.ts packages/core/tests/anthropic-provider.test.ts packages/core/tests/config.test.ts packages/core/tests/permissions.test.ts packages/core/tests/powershell.test.ts packages/core/tests/shell-tools.test.ts packages/core/tests/runtime.test.ts` 通过，8 个测试文件、71 个测试。
- Subagent review fix TDD：先观察 `packages/core/tests/agents.test.ts`、`packages/sdk/tests/package-metadata.test.ts packages/sdk/tests/agenthub-events.test.ts` 和 `pnpm contract:check` 红灯，再修复为绿灯；复测 `packages/core/tests/agents.test.ts` 通过 5 个测试，SDK/AgentHub package/event/sdk/example 组合通过 4 个测试文件、61 个测试。
- `pnpm contract:check`：通过，覆盖 package manifest、AgentHub readiness、event schema/source event type、SDK facade public type re-export 和 pack smoke 入口。
- `pnpm verify`：typecheck 通过，Vitest 26 个测试文件、240 个测试通过。
- `pnpm release:next:check`：`contract:check`、`verify`、build、core/sdk/cli dry-run pack 和 tarball install smoke 全部通过。
- `pnpm wave5:status -- --json`：10 个 Wave 5 worktree 均 clean，分支正确，`0f631f3` 是各自 HEAD 的祖先。
- `git diff --check`：通过。

当前 npm 状态：本地 prerelease gate 已经形成稳定基线，但没有执行 `npm publish`。真正发布仍需要 release owner 明确批准、确认 npm 登录/2FA/包名占用/CHANGELOG/tag 策略，并在发布后做 install smoke。

## Post-Wave 5 Stabilization Slice

本轮在 W24-W33 已合入基线之上做小步硬化，没有新建 Codex 对话线程，也没有发布 npm、触碰生产或写入 secret。

- CLI 顶层命令新增 `TOP_LEVEL_COMMAND_METADATA` registry，集中维护 `id/category/title/usage/aliases/json`，`printHelp()` 和 command handler 测试都从 registry 派生，减少 help、别名和 AgentHub command surface 漂移。
- 滚动 renderer 的 `tool.permission` 行改为 tool-first：`[permission] run_powershell requires_approval ...`，和 tool started/failed/completed 的扫描顺序一致；structured permission reason 仍保留 risk/action/mode metadata。
- `pack:smoke` 在安装真实 tarball 后扫描已安装 `@tokendance/*` package 中的可读文件，并跟随 pnpm scoped package symlink；扫描结果为 0 个可读包文件会失败，阻止 Windows user path、本地 workspace path、npm token、npm `_authToken` 和 private key material 进入 npm 包。
- 活跃端到端验收清单改为 TS 分支口径，AgentHub `agent.stream` 明确 v2 required fields；旧 Python v0.1 手工验收移动到历史附录，不再作为活跃 release gate。

Post-Wave 5 focused verification：

- `pnpm test packages/cli/tests/main.test.ts packages/cli/tests/renderer.test.ts packages/sdk/tests/package-metadata.test.ts`：通过，3 个测试文件、59 个测试。
- `pnpm contract:check`：通过。
- `pnpm verify`：typecheck 通过，Vitest 26 个测试文件、242 个测试通过。
- `pnpm pack:smoke`：通过，真实安装 core/sdk/cli tarball 后执行 SDK/CLI smoke，并验证 package privacy scan 在 pnpm symlink 安装布局下不是 no-op。
- `pnpm release:next:check`：`contract:check`、`verify`、build、core/sdk/cli dry-run pack 和 tarball install smoke 全部通过。
- `pnpm wave5:status -- --json`：10 个 Wave 5 worktree 均 clean。
- `git diff --check`：通过。

## Wave 6 Dispatch

Wave 6 已从 `8af8ab1` 切出 7 个并行 worktree，并派发 multiagent subagent 工作。当前主线只记录调度、文档治理和 release readiness，不在主 worktree 直接改 worker 文件。

| Label | Branch | Worktree | Agent | Status |
|---|---|---|---|---|
| `W34-Architecture-Compare` | `codex/wave6-architecture-compare` | `.worktrees/wave6-architecture-compare` | Euclid `019eaba0-95b6-7903-a10d-835d3a1e6f32` | merged `0582f24`, then reconciled with completed W35-W40 state |
| `W35-Slash-Command-Registry` | `codex/wave6-slash-command-registry` | `.worktrees/wave6-slash-command-registry` | Heisenberg `019eaba0-cb3f-7550-bff1-c37eb41cb610` | merged `5736b0d` via `merge wave6 slash command registry` |
| `W36-Instruction-Discovery` | `codex/wave6-instruction-discovery` | `.worktrees/wave6-instruction-discovery` | Kierkegaard `019eaba1-0693-7f32-8c26-99293f01bca4` | merged `a76022b` via `merge wave6 instruction discovery` |
| `W37-Provider-History` | `codex/wave6-provider-history` | `.worktrees/wave6-provider-history` | Turing `019eaba1-4096-7cc2-b5be-778884aa5a37` | merged `87769b2` via `merge wave6 provider history` |
| `W38-SDK-Runner-Public` | `codex/wave6-sdk-runner-public` | `.worktrees/wave6-sdk-runner-public` | James `019eaba1-8513-7db1-8450-5f8d534f2e10` | merged `c390842` via `merge wave6 sdk runner facade` |
| `W39-Permission-Profiles` | `codex/wave6-permission-profiles` | `.worktrees/wave6-permission-profiles` | Harvey `019eaba1-c692-7e12-8d1a-bc8409462703` | merged `2519de3` via `merge wave6 permission profiles`, with README conflict reconciliation |
| `W40-CLI-TUI-Brand` | `codex/wave6-cli-tui-brand` | `.worktrees/wave6-cli-tui-brand` | Galileo `019eaba2-0236-7970-af8f-158a78ddc6ed` | merged `f051cf0` via `merge wave6 cli tui brand`, with CLI registry conflict reconciliation |

Release status after dispatch：`docs/release-readiness.md` records the first npm candidate and the current npm registry `E404` state. Do not claim `@tokendance/code-*` packages are published until `npm view` returns the expected version and dist-tags.

W36 verification after merge：

- `pnpm test packages/core/tests/context-builder.test.ts packages/core/tests/runtime.test.ts`：通过，2 个测试文件、11 个测试。
- `pnpm typecheck`：通过。

Wave 6 integration verification so far：

- W37：`pnpm test packages/core/tests/anthropic-provider.test.ts packages/core/tests/openai-chat-provider.test.ts packages/core/tests/openai-provider.test.ts` 通过，3 文件、34 测试；`pnpm typecheck` 通过。
- W38：`pnpm test packages/sdk/tests/sdk.test.ts packages/sdk/tests/agenthub-events.test.ts packages/agenthub-example/tests/agenthub-runner.test.ts` 通过，3 文件、55 测试；`pnpm typecheck` 通过。
- W35：`pnpm test packages/cli/tests/main.test.ts` 通过，1 文件、45 测试；CLI package `tsc` 通过。
- W40：`pnpm test packages/cli/tests/main.test.ts packages/cli/tests/renderer.test.ts` 通过，2 文件、52 测试；CLI package `tsc` 通过。
- W39：`pnpm test packages/core/tests/permissions.test.ts packages/core/tests/tools.test.ts packages/sdk/tests/sdk.test.ts packages/sdk/tests/package-metadata.test.ts` 重跑通过，4 文件、54 测试；`pnpm typecheck` 通过。

Real Gateway CLI smoke：

- 本地 Gateway smoke env 放在 ignored `.config/tokendance/gateway-smoke.env`，`.gitignore` 已覆盖 `.config/`；状态文档不记录 key 或 secret 文件内容。
- `pnpm --filter @tokendance/code-cli build`：通过。
- `node packages/cli/dist/main.js doctor --json` 和 `config validate --json`：provider 为 `openai-chat-completions`，model 来自 ignored local smoke env，provider readiness 为 true，API key/base URL 只显示 present/env 名。
- `node packages/cli/dist/main.js run ...` 真实 Gateway smoke：configured smoke models 均返回预期 sentinel；输出没有打印 API key。

## Post npm-candidate hardening

本轮继续使用 multiagent subagent 做只读审查，不新建 Codex 对话线程。主线完成两个稳定化切片：

- AgentHub failure terminal frame：core runtime 在 provider/runtime 抛错时先持久化并发出 `turn.failed`，SDK 将其映射为 AgentHub `run.agent.result` 且 `success=false`，runner 仍 rethrow 原始错误给调用方。AgentHub 不会在失败 run 上只看到中途事件而没有终态。
- 发布隐私与公开口径：`.env.example` 改为受控 shell / SDK env / user-global `~/.tokendance/.env` 示例，明确项目根 `.env` 默认不读取；`.tmp/` 进入 ignore；tarball privacy scan 扩展 OpenAI-style key、GitHub token、provider API key assignment；release docs 不再把历史测试数量或内部 smoke 模型名当作公开证明。

本轮验证：

- `pnpm test packages/core/tests/runtime.test.ts packages/sdk/tests/agenthub-events.test.ts packages/agenthub-example/tests/agenthub-runner.test.ts packages/sdk/tests/package-metadata.test.ts`：通过，4 个测试文件、38 个测试。
- `pnpm typecheck`：通过。
- `pnpm pack:smoke`：通过，npm 原生安装真实 tarball 后执行 SDK/CLI/AgentHub fixture smoke，扩展 privacy scan 未误报。
- `pnpm contract:check`：通过。
- `pnpm registry:next:check`：core/sdk/cli 在 npmjs 仍为 `E404`，first publish candidate 仍需 release owner 人工批准。
- `pnpm release:next:check`：通过，Vitest 26 个测试文件、258 个测试，build、dry-run pack 和 tarball smoke 全部通过。
