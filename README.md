# Tokendance

Tokendance is a local command-line coding agent for personal development
workflows. The project is currently in early staged development.

## Development

Phase 1 focuses on the Python package scaffold, the global `tokendance`
command, `--version`, `doctor`, and baseline tests.
