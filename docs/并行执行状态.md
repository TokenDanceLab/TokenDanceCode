# TokenDanceCode Progress Log

更新时间：2026-06-09

This is the public progress log for the TypeScript refactor branch. It records user-visible changes, merge commits, and verification commands. Private worker names, agent IDs, local worktree paths, and operator-only secret paths are intentionally omitted.

## Current Baseline

- Branch: `codex/ts-refactor`
- Candidate version: `0.2.0-ts.0`
- Release branch pointer: `release/npm-first`
- Public npm status: release-review ready, not published. Registry checks still return `E404` for `@tokendance/code-core`, `@tokendance/code-sdk`, and `@tokendance/code-cli`.
- Current package gate: `pnpm release:next:check`
- Current AgentHub contract: `agenthub-sdk.v1`, `agent.stream` schema version `2`, terminal failures mapped to `run.agent.result` with `success=false`.

## Integrated Work

| Wave | User-visible change | Merge or baseline commit | Verification |
|---|---|---|---|
| Initial TS baseline | Added TypeScript packages, mock runtime, CLI entrypoint, SDK facade, transcript storage, and provider-neutral runtime shape | `9026166`, `f49fbc0`, `ce99022` | `pnpm verify`, `pnpm pack:check`, `git diff --check` |
| Wave 1 | Added provider hardening, safety permissions, thread/task APIs, SDK AgentHub surface, CLI renderer improvements, and package release gates | `17e448c` | `pnpm release:next:check` |
| Wave 2 | Added CLI quickstart, renderer polish, session listing, and copyable AgentHub fixture follow-up | `42dd090` | `pnpm release:next:check` |
| Wave 3 | Strengthened provider diagnostics, permission evidence, thread management, SDK contract readiness, CLI JSON checks, and release docs | `c70bc6f` | `pnpm release:next:check`, built CLI temp-home smoke |
| Wave 4 | Reduced CLI command complexity, improved AgentHub consumer fixture, added provider smoke gates, permission audit, and session lifecycle metadata | `1f4efc2` | focused suites, `pnpm verify`, `pnpm release:next:check`, `pnpm wave4:status -- --json` |
| Wave 5 | Added reference-architecture decisions, npm baseline gates, SDK feature flags, Gateway quickstart, TokenDanceID OIDC helper, provider hardening, permission safety, renderer output, and session metadata | `b590271` plus follow-up fixes | focused suites, `pnpm contract:check`, `pnpm verify`, `pnpm release:next:check`, `pnpm wave5:status -- --json` |
| Wave 6 | Added command metadata registry, instruction discovery, provider history normalization, public SDK runner facade, permission profiles, CLI brand polish, and release privacy hardening | `34e93cc` | focused suites, `pnpm typecheck`, `pnpm pack:smoke`, `pnpm contract:check`, `pnpm registry:next:check`, `pnpm release:next:check` |
| Wave 7 W41 | Added interactive CLI approval for default-mode write/shell/network/dangerous tools, including one-call allow, one-call deny, and current-session allow by tool name. Top-level `tokendance run` stays noninteractive. | `c17c973`, `1cc9120` | `pnpm test packages/cli/tests/main.test.ts packages/core/tests/runtime.test.ts`, `pnpm typecheck` |

## Release Candidate Evidence

Latest known local release-review gate:

- `pnpm test packages/core/tests/runtime.test.ts packages/sdk/tests/agenthub-events.test.ts packages/agenthub-example/tests/agenthub-runner.test.ts packages/sdk/tests/package-metadata.test.ts` passed.
- `pnpm typecheck` passed.
- `pnpm pack:smoke` passed with real packed core, SDK, and CLI tarballs installed into a temporary npm project.
- `pnpm contract:check` passed.
- `pnpm registry:next:check` reported npm `E404`, which is valid for first publish but proves the packages are not public yet.
- `pnpm release:next:check` passed with build, Vitest, dry-run pack, and tarball install smoke.

Use fresh command output as the source for current test counts. Do not copy old counts into release notes.

## Active Wave 7 Work

Wave 7 starts from `34e93cc` and focuses on local CLI/SDK behavior that improves daily use without expanding the product boundary into a cloud daemon, IDE integration, plugin marketplace, or hosted collaboration system.

| Workstream | User-visible goal | Status |
|---|---|---|
| Local CLI approval | Interactive CLI can approve or deny default-mode write/shell actions instead of stopping at `requires_approval`; current-session allow is supported per tool name | Merged |
| Structured run output | `tokendance run` can emit script-friendly JSON / stream JSON for final response, tool events, permission decisions, and failures | In progress |
| Subject-level permissions | Path and shell command subjects add explicit ask/deny evidence beyond coarse tool risk | Completed in worker branch, pending coordinator review |
| Context budget recovery | Context builder gets explicit budgets and recoverable compact snippets for long tasks | In progress |
| Deterministic local hooks | Local JSON hooks cover selected tool and turn lifecycle events with fixed timeout/failure policy | In progress |
| Same-session AgentHub concurrency | Same `sessionId` concurrent runs are serialized or rejected with a structured result | In progress |

Maintainer status check:

```powershell
pnpm wave7:status -- --json
pnpm wave7:status -- --strict-clean
```

The default status check is read-only and allows dirty worker branches while implementation is active. Use `--strict-clean` before merging a workstream.

## Public Boundaries

- npm publish is not automated and must not be part of verification scripts.
- TokenDance Gateway model calls use TokenDance API keys. TokenDanceID/OIDC is for identity/session flows in consuming products.
- Public docs may name environment variables but must not include real keys, token-bearing config files, local operator paths, or production logs.
- Old Python files are retained as migration reference only. Current development targets the TypeScript packages.
